#include <stdio.h>

/**
 * main - this is the entry point of the function
 *
 * Return: returns value of 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
